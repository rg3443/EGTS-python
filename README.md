# EGTS-python
# egts-python (Python 3 Fork)  **A modern, Python 3-compatible fork of the original `egts-python` library** — a lightweight implementation of the **EGTS (European Geostationary Navigation Overlay Service Transport Protocol)** in pure Python.
